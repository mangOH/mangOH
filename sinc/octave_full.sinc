#include "octave_minimal.sinc"

apps:
{
    $OCTAVE_ROOT/io.${LEGATO_TARGET}.app
#if ${MANGOH_BOARD} = red
    $OCTAVE_ROOT/lcd.${LEGATO_TARGET}.app
#endif
    $OCTAVE_ROOT/location.${LEGATO_TARGET}.app
    $OCTAVE_ROOT/modbus.${LEGATO_TARGET}.app
    $OCTAVE_ROOT/remote.${LEGATO_TARGET}.app
    $OCTAVE_ROOT/util.${LEGATO_TARGET}.app
}
