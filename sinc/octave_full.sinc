#include "octave_minimal.sinc"

apps:
{
    $CURDIR/../apps/octave/io.${LEGATO_TARGET}.app
#if ${MANGOH_BOARD} = red
    $CURDIR/../apps/octave/lcd.${LEGATO_TARGET}.app
#endif
    $CURDIR/../apps/octave/location.${LEGATO_TARGET}.app
    $CURDIR/../apps/octave/modbus.${LEGATO_TARGET}.app
    $CURDIR/../apps/octave/remote.${LEGATO_TARGET}.app
}